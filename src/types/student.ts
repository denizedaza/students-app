export type Student = {
  city: string,
  company: string,
  email: string,
  firstName: string,
  grades: string[],
  id: string,
  lastName: string,
  pic: string,
  skill: string,
  average: number,
  tags: string[]
}